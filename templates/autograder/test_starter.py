from starter import solve


def test_solve_runs():
    # Placeholder - replace tests/ with the assignment's real tests. Fails on the
    # un-implemented starter (fresh submission scores 0), passes once solved.
    assert solve() is not None
