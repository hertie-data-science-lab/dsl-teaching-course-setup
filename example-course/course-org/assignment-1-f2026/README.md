# Assignment 1 - Linear models

Implement and evaluate a linear regression model on the provided dataset.

## Tasks
1. Complete `solution()` in `starter.py`.
2. Report train/test error in a short markdown cell.

## Submission
Push your work to `main` in your private `assignment-1-<your-handle>` repo. That's it -
no CLI, no "accept" step. The autograder (when enabled) runs on push.

_This README is the assignment brief shown on the cohort website._
